{% extends 'base.html.twig' %}

{% block title %}Edit Owner{% endblock %}

{% block body %}
    <h1>Edit Owner</h1>

    {{ include('owner/_form.html.twig', {'button_label': 'Update'}) }}
    <br/>
    <a class="btn btn-primary" href="{{ path('owner_index') }}">back to list</a>

    {{ include('owner/_delete_form.html.twig') }}
{% endblock %}
